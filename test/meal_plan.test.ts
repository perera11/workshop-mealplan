// import handler from "../lib/lambda/getLambda";

// describe("handler", () => {
//   it("returns a successful response", async () => {
//     const event = {
//       body: JSON.stringify({
//         name: "John Doe",
//         age: 30,
//       }),
//     };

//     const context = {};

//     const response = await handler(event, context);

//     expect(response.statusCode).toBe(200);
//     expect(response.body).toBe(
//       JSON.stringify({ message: "Hello John Doe, your age is 30" })
//     );
//   });
// });
